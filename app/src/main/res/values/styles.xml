<resources>

   <!-- Base application theme. -->
    <style name="AppTheme" parent="@style/Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/sunshine_blue</item>
        <item name="colorPrimaryDark">@color/sunshine_dark_blue</item>
    </style>

    <!-- Main activity theme. -->
    <style name="ForecastTheme" parent="AppTheme">
        <item name="actionBarTheme">@style/ActionBar.Solid.Sunshine.Logo</item>
        <item name="actionBarPopupTheme">@style/Popup.Sunshine</item>
    </style>

    <!-- Detail activity theme. -->
    <style name="DetailTheme" parent="AppTheme">
        <item name="actionBarTheme">@style/ActionBar.Solid.Sunshine.Title</item>
        <item name="actionBarPopupTheme">@style/Popup.Sunshine</item>
    </style>

    <!-- Settings activity theme. -->
    <style name="SettingsTheme" parent="AppTheme">
        <item name="actionBarTheme">@style/ActionBar.Solid.Sunshine</item>
        <item name="actionBarPopupTheme">@style/Popup.Sunshine</item>
        <item name="preferenceTheme">@style/PreferenceThemeOverlay</item>
    </style>

    <!-- main activity action bar styles -->
    <style name="ActionBar.Solid.Sunshine" parent="@style/ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="android:background">@color/sunshine_blue</item>
        <item name="displayOptions">showHome|homeAsUp</item>
    </style>
    <style name="ActionBar.Solid.Sunshine.Logo">
        <item name="displayOptions">useLogo</item>
        <item name="logo">@drawable/ic_logo</item>
    </style>
    <style name="ActionBar.Solid.Sunshine.Title">
        <item name="displayOptions">showTitle</item>
    </style>
    <style name="Popup.Sunshine" parent="@style/ThemeOverlay.AppCompat.Light">
        <item name="android:background">@android:color/white</item>
    </style>

    <!-- Style for forecast listview -->
    <style name="ForecastListStyle">
        <!-- Here it's a stub.  In screens of sufficient width, this style includes modifications
        for two-pane layout -->
    </style>

</resources>